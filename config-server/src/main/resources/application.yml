server:
  port: 8888

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:../config-repo

          # /Users/crystalzhao95/react-tabs-hw/forum-project/config-repo